<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.qut.dao.app.GradeAndroidDao">
	  <!-- 	//密码修改 -->
   <select id="findCustomGrade" resultType="cn.edu.qut.entity.app.Grade">
   		SELECT * FROM grade WHERE customer_id = #{customer_id} AND store_id = #{store_id}
   </select>
</mapper>